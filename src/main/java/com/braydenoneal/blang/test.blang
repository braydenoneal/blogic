/*
Multi-line comment
*/

// Comment

pub var num1 int = 12;

pub fn test(a int) int {
    num + a
}

pub struct Foo {
    foo int,
    bar bool
}

impl Foo {
    pub fn new() Self {
        Self(default_foo(), false)
    }

    fn default_foo() int {
        0
    }
}

fn main() {
    foo Foo = Foo.new();
    foo.foo = test(2);
}

fn tick() {
    var something bool = true;
}
