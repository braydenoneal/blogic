/*
Multi-line comment
*/

// Comment

// Variables are always private, but can be exposed through pub or prot functions
var num1 = 12;

pub fn test(a) {
    num + a
}

pub struct Foo {
    foo,
    bar,
}

impl Foo {
    pub fn new() {
        Self(default_foo(), false)
    }

    fn default_foo() {
        0
    }
}

fn main() {
    foo Foo = Foo.new();
    foo.foo = test(2);
}

fn tick() {
    var something = true;
}
